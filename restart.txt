For restarting the stuff

export LC_ALL=C.UTF-8
export LANG=C.UTF-8
export FLASK_APP=flaskr
export FLASK_ENV=development
flask run --host=0.0.0.0 --port=3000

python3 run -m flask --host=0.0.0.0 --port=3000
